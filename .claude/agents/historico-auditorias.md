# Histórico de auditorías — dashboard-414

Log append-only, una fila por corrida. Si el mismo agente corre dos veces
el mismo día, se complementa la fila de esa fecha en vez de duplicarla.

| Fecha | Agente | Críticos | Menores | Resumen |
| --- | --- | --- | --- | --- |
| 2026-08-01 | limpieza-414 | 0 | 4 | Proyecto limpio: solo boilerplate shadcn sin uso (Progress/Select subcomponentes, badge/buttonVariants) y `tw-animate-css`/`shadcn` mejor en devDependencies; sin desviaciones de git ni documentación desactualizada. |
| 2026-08-01 | buenas-practicas-414 | 0 | 1 | Separación de responsabilidades y roadmap↔código sin hallazgos (Fase 3a, `refresh()`, `params` async, sin xlsx local); único hallazgo: `kpi-strip.tsx` arma clases condicionales con template literals en vez de `cn()`. |
| 2026-08-08 | buenas-practicas-414 | 0 | 5 | Post-Fase A (Supabase)/Unidad 0.x: convenciones Next.js/React sin hallazgos nuevos (el hallazgo previo de `kpi-strip.tsx` ya no aplica, usa `cn()`); 5 Menores de documentación desactualizada — tabla "Archivos clave" no menciona `requerimiento-data.ts` ni `database.types.ts`, no refleja avance parcial de Fase 0 (tipos generados, tests Vitest, scripts Supabase CLI), y sigue nombrando `archivo-bloqueado-banner.tsx`/`ArchivoBloqueadoBanner` (hoy `error-datos-banner.tsx`/`ErrorDatosBanner`); más duplicación literal de `FASES_ORDEN` y de la consulta "proyecto por slug" en tres archivos de `lib/`. |
| 2026-08-08 | limpieza-414 | 2 | 6 | Post-Fase A (Supabase)/Unidad 0.x: 2 Críticos de documentación — `README.md` completo describe el estado pre-Fase A (Google Sheet/xlsx, sin BD, Fase 3a) contradiciendo a `CLAUDE.md`; `CLAUDE.md` sigue apuntando a `supabase/schema.sql` como el DDL a correr en el SQL Editor, pero ese archivo se movió a `supabase/legado/` (histórico, no ejecutar) y la verdad vigente son las migraciones versionadas. 6 Menores: referencias a `archivo-bloqueado-banner.tsx`/`ArchivoBloqueadoBanner` (hoy `error-datos-banner.tsx`/`ErrorDatosBanner`), tabla "Archivos clave" desactualizada sobre `requerimiento-data.ts` y el avance de Fase 0, plan histórico referenciado que ya no existe, y boilerplate shadcn sin uso (incluye `sheet.tsx`, nuevo desde la Fase A). Sin hallazgos de higiene de git ni de `package.json` (el hallazgo de la corrida anterior ya está resuelto). |
| 2026-08-09 | limpieza-414 | 1 | 6 | Post-corrección documental parcial (Fase A/0 completas): 1 Crítico de inconsistencia documental — `README.md` línea 20 dice "Fase 0 (fundaciones — en curso" pero `CLAUDE.md` línea 8 declara "✅ completa (2026-08-09)". 6 Menores: `zod` en dependencies sin uso actual (andamiaje para Fases C/D), 5 exports de subcomponentes shadcn sin consumidor externo (progress, select, sheet, badge, button). Hallazgos Críticos previos de `schema.sql` y `archivo-bloqueado-banner.tsx` mejorados documentacionalmente (claros/diferenciados hoy). |
| 2026-08-09 | buenas-practicas-414 | 2 | 0 | Post-Fase 0 (Fundaciones) ✅ completa según `CLAUDE.md` (línea 11, 2026-08-09): el código de `src/` está sin hallazgos (separación de responsabilidades, convenciones Next.js/React, uso de `cn()` en clases condicionales, `params` async, `refresh()`, iconos como componentes reales, sin `xlsx`/`fs`). 2 Críticos documentales idénticos a hallazgo de `limpieza-414` hoy — `README.md` contradice a `CLAUDE.md`: línea 20 dice "Fase 0 en curso" (incorrecto) y línea 9 dice "Fase A" (nomenclatura desincronizada); ambas inducen a creer que Fase 0 está pendiente cuando ya está completa. |
| 2026-08-12 | limpieza-414 | 0 | 1 | Post-cierre técnico pre-refinamiento 2026-08-11 (fusión tarea/actividad, eliminación document_versions, partición tarea-acciones-admin): documentación muy consistente. Tabla "Archivos clave" precisa; todos los archivos mencionados existen; referencias a eliminados documentadas con contexto. Sin hallazgos críticos ni de higiene de git. 1 Menor: 12 tokens CSS boilerplate de shadcn (sidebar/chart) sin consumidor actual — infraestructura válida para expansión futura (regla #6 del agente). Hallazgo anterior de `zod` sin uso (2026-08-09) ya resuelto de facto — se importa y usa en validación de Server Actions. |
| 2026-08-12 | buenas-practicas-414 | 1 | 1 | Post-cierre técnico pre-refinamiento 2026-08-11: auditoría exhaustiva de `useActionState` y arquitectura newos/partidos. **1 Crítico:** `estado-tarea-select.tsx` (líneas 24-32) invoca `estadoFormAction` desde `onValueChange` sin `startTransition()` — patrón incorrecto que viola la convención de React cuando dispatch de `useActionState` no está dentro de `<form action>` nativo. **1 Menor:** `eliminar-tarea-button.tsx` (línea 44) usa `router.refresh()` en lugar de `refresh()` de `next/cache`, desviándose de la convención documentada. Separación de responsabilidades, consistencia roadmap↔código, y demás convenciones Next.js/React sin hallazgos. Revisión de 10 usos de `useActionState`: solo 1 incorrecto. |

**Punto de control documental (2026-08-09, mismo día de las dos corridas de arriba)**: el PO pidió una revisión no superficial de todo el proyecto antes de arrancar Fase B. El Crítico de `README.md` (Fase 0 "en curso" vs. "completa") reportado por ambos agentes arriba **se corrigió en el mismo punto de control**. Además: se añadió una sección `## Backup` dedicada en `CLAUDE.md` (antes disperso dentro de "Estado actual"), con instrucción explícita de vigilar el auto-apagado de workflows programados de GitHub a los 60 días de inactividad; y se recortó `ROADMAP_SUPABASE.md` (424→~30 líneas) a solo su resumen ejecutivo ("Cierre de Fase A"), ya que `ROADMAP_V2.md` cubre el diseño completo y vigente de Fase B/C/D. Los 6 Menores de exports sin uso de shadcn/Base UI y la duplicación de `FASES_ORDEN`/"proyecto por slug" quedaron sin tocar (decisión explícita del PO: son candidatos ya trackeados para la unidad de "andamiaje compartido" de `ROADMAP_V2.md`, no alcance de este punto de control documental).
